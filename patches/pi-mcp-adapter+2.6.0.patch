diff --git a/node_modules/pi-mcp-adapter/init.ts b/node_modules/pi-mcp-adapter/init.ts
index df522ce..475af88 100644
--- a/node_modules/pi-mcp-adapter/init.ts
+++ b/node_modules/pi-mcp-adapter/init.ts
@@ -266,9 +266,17 @@ export function flushMetadataCache(state: McpExtensionState): void {
   }
 }
 
+function hideStatusBar(state: McpExtensionState): boolean {
+  return (state.config.settings as { hideStatusBar?: boolean } | undefined)?.hideStatusBar === true;
+}
+
 export function updateStatusBar(state: McpExtensionState): void {
   const ui = state.ui;
   if (!ui) return;
+  if (hideStatusBar(state)) {
+    ui.setStatus("mcp", undefined);
+    return;
+  }
   const total = Object.keys(state.config.mcpServers).length;
   if (total === 0) {
     ui.setStatus("mcp", undefined);
diff --git a/node_modules/pi-mcp-adapter/types.ts b/node_modules/pi-mcp-adapter/types.ts
index 66ca593..7f65d51 100644
--- a/node_modules/pi-mcp-adapter/types.ts
+++ b/node_modules/pi-mcp-adapter/types.ts
@@ -320,6 +320,7 @@ export interface McpSettings {
   autoAuth?: boolean;
   sampling?: boolean;
   samplingAutoApprove?: boolean;
+  hideStatusBar?: boolean;
   /**
    * Message returned in tool results when a server needs (re-)authentication.
    * "${server}" is substituted with the server name. Defaults to a TUI
